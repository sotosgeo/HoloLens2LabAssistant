using System.Collections;
using System.Collections.Generic;
using UnityEngine;



/// <summary>
/// Before the exercise starts, asks the user for what components he will need to use
/// and saves these components as the layout that will be checked against for
/// mistakes during the exercise
///
/// </summary>
public class ComponentIdentification : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
